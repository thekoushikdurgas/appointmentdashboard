import {
  buildFacetSectionChips,
  buildSearchChip,
  type EntityFilterChip,
} from "@/lib/entityFilterChips";

export type ContactFilterChip = EntityFilterChip;

export { buildFacetSectionChips, buildSearchChip as buildEmailSearchChip };
