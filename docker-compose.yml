# Local / CI convenience wrapper around the app Dockerfile (best-practices check + reproducible runs).
services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
